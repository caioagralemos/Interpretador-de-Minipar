c_channel client "localhost" 8585;

while (true)
SEQ {
    string entrada;
    entrada = input();
    if (entrada == "exit")
    SEQ {
        break;
    }
    string ret;
    ret = send(entrada, client);
    output(ret);
}
client.close();